{% extends "base.html.twig"%}

{% block title %} {{title}} {% endblock %}



{% block body %}

<h1>{{message}} </h1>



<div class="row">
  {{form_start(form)}}
      <div class="card">
        <div class="card-body">
            <div class="row">
                    <div class="col-md-3">
                    {{form_row(form.minSurface)}}
                    </div>
                    <div class="col-md-3">
                    {{form_row(form.minChambres)}}
                    </div>
                    <div class="col-md-3">
                    {{form_row(form.minCouchage)}}
                    </div>
                    <div class="col-md-3">
                     {{form_row(form.submit)}}
                    </div>
            </div>
        </div>
      </div>
    {{form_end(form)}}
</div>



<div class="row">

{% for gite in gites %}
        <div class="col-md-4">


<div class="card" style="width: 18rem;">
  <img src="..." class="card-img-top" alt="...">
  <div class="card-body">
    <h5 class="card-title">{{gite.nom}}</h5>
    <p class="card-text"> 
    {{gite.descriptiion}}<br>
{{gite.surface}} m² - {{gite.chambres}} ch - {{gite.couchage}} lits
    </p>
    
    <a href="{{path('gite_show', {id: gite.id})}}" class="btn btn-primary">Voir le gite</a>
  </div>
</div>
</div>
{% endfor %}
</div>

{% endblock %}



