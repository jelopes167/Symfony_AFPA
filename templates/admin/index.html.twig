{% extends "dashboard.html.twig" %}


{%block title %} Admin {% endblock %}



{% block body %}

<h1> Gestion des gites </h1>

    <div class="form-group">
    <a href="#" class="btn btn-success">Créer un gite</a>
    </div>



<table class="table table-striped">
                <thead>
                    <tr>
                        <td>#</td>
                        <td>Nom du gite</td>
                        <td>Actions</td>
                    </tr>
                </thead>
            <tbody>
                {% for gite in gites %}
                    <tr>
                        <td>
                                {{gite.id}}
                        </td>
                        <td> 
                                {{gite.nom}} 
                        </td>
                        <td>    
                                <button> Editer </button>
                                <button> Supprimer </button>
                        </td>
                    </tr>
                {% endfor %}

            </tbody>
</table>
{% endblock %}